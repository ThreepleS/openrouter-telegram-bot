"""PWA + Telegram Mini App layer.

Reuses the existing AI/database logic so the web client and the Telegram bot
share the same backend. See ``app.py`` (server factory) and ``routes.py``.
"""

from __future__ import annotations
